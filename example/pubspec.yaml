name: imcodec_native_example
description: Demonstrates optional codecs and background conversion on every Flutter platform.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  imcodec_native:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
